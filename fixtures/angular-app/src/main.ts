import { bootstrapApplication } from '@angular/platform-browser';
import { FixtureCardComponent } from './fixture-card.component.js';

bootstrapApplication(FixtureCardComponent).catch((error: unknown) => console.error(error));
