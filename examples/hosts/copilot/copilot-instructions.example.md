# Smart UI Validator instructions

Use the Smart UI MCP server as the single orchestration surface. Start with `inspect_project` and
`plan_component`, then validate. Ask before mutating tools and keep `allowWrite` exact. Never copy
private screenshots, source, design content, or memory outside the workspace.
